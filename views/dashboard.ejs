<% const renderedBody = include('partials/dashboard-body', { 
    plants, 
    dailyTasks, 
    currentMonth, 
    selectedDate 
}); %>

<%- include('layouts/main', {
    title: 'Dashboard',
    showSidebar: true,
    active: 'dashboard',
    body: renderedBody
}) %>
